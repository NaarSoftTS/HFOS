# Exports

_Ship stub only._

Finished artifacts land at `@resources/exports/<workspace>/` (or the install’s ship store from the adapter).  
This folder is not the brain and not the canonical ship shelf.

When an adapter points ship in-tree for a self-contained install, still keep this README as the rule — and put real files under a workspace subfolder, not loose at root.
