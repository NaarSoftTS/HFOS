# Inbox

_Capture only. Nothing lives here._

Drop raw notes, forwards, and “file this later” material.  
On process: route to the owning subject (workspace STATE, person file, momentum, or companion SOURCE) — then clear or move to processed/cold store per install norms.

**Do not** treat inbox as memory or strategy.
